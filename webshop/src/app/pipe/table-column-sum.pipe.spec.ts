import { TableColumnSumPipe } from './table-column-sum.pipe';

describe('TableColumnSumPipe', () => {
  it('create an instance', () => {
    const pipe = new TableColumnSumPipe();
    expect(pipe).toBeTruthy();
  });
});
